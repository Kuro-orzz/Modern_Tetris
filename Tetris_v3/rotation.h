#ifndef ROTATION_H
#define ROTATION_H

#include "block.h"
#include "Game.h"
#include "gVar.h"

bool Can_Rotate_Right(shape cur);
bool Can_Rotate_Left(shape cur);
bool Can_Rotate_180(shape cur);

#endif // ROTATION_H
